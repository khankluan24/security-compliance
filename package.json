{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.13"
  },
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat ignition deploy",
    "verify": "hardhat ignition verify",
    "test": "hardhat test"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.5"
  }
}
